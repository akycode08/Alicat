# 📋 Чеклист для Unit Testing - Alicat Controller

## 🎯 Приоритет 1: Критичные функции (обязательно)

### 1. Валидация давления (Target Pressure)
- [ ] **ValidateTargetAgainstMax()** - проверка превышения Max Pressure
  - Значение выше максимума → красный цвет
  - Значение ниже максимума → нормальный цвет
  - Значение равно максимуму → нормальный цвет
  - Пустое значение → нормальный цвет
  - Невалидное значение → нормальный цвет

- [ ] **GoToTarget()** - валидация при установке target
  - Значение выше Max Pressure → предупреждение, не устанавливается
  - Значение ниже Min Pressure → предупреждение, не устанавливается
  - Значение в пределах Min-Max → устанавливается успешно
  - Пустая строка → предупреждение
  - Невалидный формат → ошибка
  - Устройство не подключено → предупреждение

### 2. Валидация инкремента
- [ ] **IncrementMinus()** - уменьшение инкремента
  - Уменьшение на 0.1 в пределах Min-Max → успешно
  - Попытка уменьшить ниже Min Increment → предупреждение, не изменяется
  - Значение на границе Min → не уменьшается

- [ ] **IncrementPlus()** - увеличение инкремента
  - Увеличение на 0.1 в пределах Min-Max → успешно
  - Попытка увеличить выше Max Increment → предупреждение, не изменяется
  - Значение на границе Max → не увеличивается

- [ ] **UpdateIncrementFromText()** - изменение инкремента через ввод
  - Валидное значение в пределах Min-Max → устанавливается
  - Значение ниже Min → зажимается до Min
  - Значение выше Max → зажимается до Max
  - Невалидный формат → игнорируется
  - Пустая строка → игнорируется

### 3. Операции с давлением
- [ ] **Increase()** - увеличение давления
  - Увеличение в пределах Max → успешно
  - Попытка превысить Max → предупреждение, не увеличивается
  - Устройство не подключено → предупреждение

- [ ] **Decrease()** - уменьшение давления
  - Уменьшение в пределах Min → успешно
  - Попытка уменьшить ниже Min → предупреждение, не уменьшается
  - Устройство не подключено → предупреждение

### 4. Парсинг команд устройства
- [ ] **TryParseAls()** - парсинг ответа ALS
  - Валидный формат: "A +0050.0 +0050.0 10 PSIG" → успешно
  - Валидный формат с отрицательным значением → успешно
  - Невалидный формат → false
  - Пустая строка → false
  - Разные единицы измерения (PSI, bar, Pa) → успешно

- [ ] **TryParseAsr()** - парсинг ответа ASR (Ramp Speed)
  - Валидный формат: "A 5.0 PSIG/s" → успешно
  - Валидный формат с разными единицами → успешно
  - Невалидный формат → false
  - Пустая строка → false

---

## 🎯 Приоритет 2: Важные функции (рекомендуется)

### 5. Настройки (Preferences)
- [ ] **TryApplyFromUi()** - валидация настроек
  - Все поля валидны → успешно
  - Отрицательные значения → ошибка
  - Min > Max для Pressure → ошибка
  - Min > Max для Increment → ошибка
  - Пустые значения → успешно (null)
  - Невалидный формат чисел → ошибка

- [ ] **ApplyOptionsToUi()** - применение настроек
  - Изменение единиц измерения → обновляется UI
  - Изменение лимитов → обновляются значения
  - Изменение Ramp Speed → обновляется значение

### 6. Purge операция
- [ ] **Purge()** - продувка
  - Устройство подключено → открывает выхлоп, устанавливает 0.0
  - Устройство не подключено → предупреждение
  - После Purge выхлоп остается открытым
  - Setpoint устанавливается в 0.0 на устройстве

### 7. Pause операция
- [ ] **Pause()** - пауза рампы
  - Устройство подключено → устанавливает setpoint = current
  - Устройство не подключено → предупреждение
  - Флаг _isPaused устанавливается в true

### 8. Emergency Stop
- [ ] **EmergencyStop()** - аварийная остановка
  - Устройство подключено → сбрасывает давление до 0
  - Устройство не подключено → предупреждение
  - Подтверждение пользователя → выполняется
  - Отмена → не выполняется

---

## 🎯 Приоритет 3: Вспомогательные функции (желательно)

### 9. Утилиты форматирования
- [ ] **TrimZeros()** - удаление лишних нулей
  - 5.0 → "5"
  - 5.10 → "5.1"
  - 5.123 → "5.12" (maxDecimals = 2)
  - 0.0 → "0"

- [ ] **NormalizeUnit()** - нормализация единиц измерения
  - "PSI" → "PSIG"
  - "PSIG" → "PSIG"
  - "bar" → "bar"
  - "Pa" → "Pa"

### 10. Расчеты
- [ ] **ETA расчет** (в UI_SetTrendStatus)
  - diff = 10, rampSpeed = 5 → ETA = 2.0s
  - diff = 0 → "At target"
  - rampSpeed = 0 → показывает разницу вместо ETA

### 11. Управление сессиями
- [ ] **StartSession()** - начало сессии
  - Создает новую сессию
  - Очищает предыдущие данные
  - Устанавливает флаг IsRunning = true

- [ ] **RecordSample()** - запись данных
  - Записывает в RAM всегда
  - Записывает в CSV только при изменении >= 0.3
  - События записываются в CSV всегда

---

## 🎯 Приоритет 4: Интеграционные тесты (опционально)

### 12. Интеграция с устройством
- [ ] **ConnectDevice()** - подключение
  - Успешное подключение → статус "Connected"
  - Ошибка подключения → статус "Disconnected"
  - Отключение → статус "Disconnected"

- [ ] **Serial_LineReceived()** - обработка данных
  - Получение ALS → обновление Current и SetPoint
  - Получение ASR → обновление Ramp Speed
  - Обновление UI через BeginInvoke

### 13. Сохранение/загрузка настроек
- [ ] **SaveSettingsToFile()** - сохранение
  - Создает файл в AppData
  - Сохраняет все настройки в JSON
  - Обработка ошибок записи

- [ ] **LoadSettingsFromFile()** - загрузка
  - Загружает настройки из JSON
  - Файл не существует → использует defaults
  - Невалидный JSON → использует defaults

---

## 📝 Рекомендации по тестированию

### Структура тестов
```
Tests/
├── Unit/
│   ├── ValidationTests.cs
│   ├── ParsingTests.cs
│   ├── CalculationTests.cs
│   └── SettingsTests.cs
├── Integration/
│   ├── DeviceConnectionTests.cs
│   └── DataStoreTests.cs
└── Helpers/
    └── TestHelpers.cs
```

### Используемые фреймворки
- **NUnit** или **xUnit** - для unit тестов
- **Moq** - для моков (IMainView, ISerialClient)
- **FluentAssertions** - для читаемых assertions

### Пример структуры теста
```csharp
[Test]
public void IncrementPlus_WithinLimit_IncrementsByPointOne()
{
    // Arrange
    var presenter = new MainPresenter(mockView, mockDataStore);
    presenter.Initialize();
    presenter.ApplyOptionsToUi();
    
    // Act
    presenter.IncrementPlus();
    
    // Assert
    Assert.AreEqual(5.1, presenter.CurrentIncrement, 0.01);
}
```

---

## ✅ Критерии готовности

- [ ] Покрытие критичных функций (Приоритет 1) - 100%
- [ ] Покрытие важных функций (Приоритет 2) - 80%
- [ ] Покрытие вспомогательных функций (Приоритет 3) - 60%
- [ ] Все тесты проходят
- [ ] Тесты запускаются автоматически в CI/CD

---

**Дата создания:** 2024
**Последнее обновление:** 2024


